server:
  port: 19097

spring:
  application:
    name: review
  profiles:
    active: dev
  config:
    import: optional:file:.env[.properties]
  #    import: "configserver:http://localhost:18080"
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    url: jdbc:postgresql://localhost:${POSTGRES_BINDING_PORT}/${POSTGRES_DB}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
  data:
    redis:
      host: localhost
      port: ${REDIS_BINDING_PORT}

reservation:
  service:
    url: http://localhost:${RESERVATION_SERVICE_PORT}


#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:19090/eureka/

#message: "default message" # config server? ???? ?? ?

#management:
#  endpoints:
#    web:
#      exposure:
#        include: refresh